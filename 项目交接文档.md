# 我的文字修仙全靠刷 · 项目交接文档（作者专用）

> 目的：以后换任何 AI 模型 / 工具 / 人，不需要聊天记录，照着这份文档就能继续开发和改进。
> 唯一信源：本 Git 仓库。任何改动都以 `git push` 后的仓库为准，不要只留在本地或聊天里。

## 0. 先记住三件事
- 仓库地址：`https://github.com/qingtang666666-source/vue-xiuxiangame.git`
- 本机源码路径：`D:\MirServer\project-candidates\vue-xiuxiangame`
- 玩家线上入口：`https://qingtang666666-source.github.io/vue-xiuxiangame/`

## 1. 项目是什么
- 名字：我的文字修仙全靠刷（内部名 `xiuxian-game`）
- 类型：单机网页游戏，Vue 3 + Vite 6 + Element Plus + Pinia，纯前端，无后端服务器。
- 数据：玩家存档存在浏览器 localStorage（pinia-plugin-persistedstate）。

## 2. 拿到源码后的开工步骤
```bash
cd vue-xiuxiangame
npm install
npm run dev        # 本地开发，自动开浏览器
```
改代码后验证：
```bash
npm run build      # 产出 dist/，并自动做混淆压缩
npm run preview    # 本地预览 dist
```
提交发布（GitHub Actions 会自动构建并部署到 Pages）：
```bash
git add -A
git commit -m "改动说明"
git push
```

## 3. 目录结构
```
src/
  main.js            # 入口：挂载 App、Pinia、持久化、Element Plus
  App.vue            # 全局壳：顶部HUD/游商/底部Dock/全功能面板/路由出口
  plugins/router.js  # 所有路由（页面地址都在这）
  plugins/store.js   # Pinia 玩家数据模型（存档结构）
  plugins/persistence.js # 存档持久化（脏标记 + 延迟落盘）
  views/             # 各个页面（约 28 个，文件名即功能）
  components/        # 复用组件（战斗、功法、面板、装备等）
  styles/theme.css   # 全局主题（配色/质感/动效）
  assets/            # 图片图标等资源
```
根目录重要文件：
- `vite.config.js`：构建配置 + 代码混淆插件 + PWA
- `.github/workflows/gh-pages.yml`：推送 main 后自动构建部署
- `serve-dist.mjs`：本机用一个 Node 命令把 dist 跑成网站
- `start-game.cmd`：双击启动本地游戏（老方式，在线版已改为直接进 GitHub Pages）
- `项目交接文档.md`：本文档

## 4. 常用模块速查
- 页面：`src/views/`，按拼音/英文见名知义：
  `homePage` 主页、`cultivatePage` 修炼突破、`battlePage` 历战、`realmPage` 秘境、
  `endlessPage` 无尽塔、`bossPage` 世界Boss、`manorPage` 洞府、`alchemyPage` 炼丹、
  `forgePage` 炼器、`talismanPage` 制符、`formationPage` 阵法、`explorePage` 探索、
  `worldMap` 大世界、`mapExploration` 洞天、`questPage` 任务、`backpackPage` 背包、
  `marketPage` 坊市、`npcPage` NPC、`sectPage` 宗门、`guildPage` 仙盟、
  `ascensionPage` 飞升、`rebirthShopPage` 转生商店、`gmPage` 调试。
- 玩法/数值逻辑：`src/plugins/`，按文件名查：
  `battleEngine.js` 回合战斗引擎、`enemyScale.js` 敌人难度缩放、`technique.js` 功法、
  `formation.js` 阵法、`alchemy.js` 炼丹、`forge.js` 炼器、`talisman.js` 符箓、
  `equip*.js` 装备体系、`quest.js` 任务、`guide.js` 新手引导、`guild.js` 仙盟、
  `npcSystem.js` NPC 体系、`sect.js` 宗门、`market.js`/`shop.js` 交易、`time.js` 时间/寿元、
  `rebirth*.js`/`rebirthFlow.js` 轮回转世、`ascension.js` 飞升、`manor.js` 洞府、
  `season.js` 赛季、`worldBoss.js` 世界Boss、`autoIdle.js` 挂机、`offline.js` 离线结算、
  `saveVault.js` 存档保险库、`celebrate.js` 突破动画。
- 通用工具 `game.js`：`formatNumberToChineseUnit`（数字转中文单位）、`levelNames`、`gameNotifys`（提示）、`propItemNames`（道具名）等。

## 5. 最近已实现/已修复（2026-09）
- 战斗 UI：`battle-arena-bg.png` 场景图 + `battlePage.vue`/`TurnCombat.vue` 接入。
- 历战难度削弱（`enemyScale.js` 的 `LADDER_DIFFICULTIES` 与 battlePage 难度倍率）。
- 突破大境界需消耗「道果」，仅历战掉落，灵石买不到（`battleEngine.js` 掉落 + `cultivatePage.vue` 校验）。
- 手机端：锁定整页视口、底部 Dock、全功能面板、炼丹/制符/阵法紧凑双列网格；电脑端加宽面板与多列布局（`App.vue` + `styles/theme.css` + 各页面 scoped 样式）。
- 修复场景切换偶发卡死：移除了 `router-view` 里的 `transition mode=out-in`（`App.vue`），保留纯 CSS 淡入。
- 桌面快捷方式指向 GitHub Pages，线上自动更新。

## 6. 发版流程（重要）
1. 只改 `src/`。`dist/` 是构建产物，**不要手改**。
2. `npm run build` 通过后再提交。
3. push 到 `main`，GitHub Actions `gh-pages.yml` 自动 npm install → build → 部署 Pages（约 1~2 分钟）。
4. 玩家刷新页面即可（有 PWA 缓存时偶尔需要多刷一次）。

> 部署现状（2026-09-08 收尾）：线上仅走 GitHub Actions（`gh-pages.yml` → `dist` → 默认 Pages 地址 `qingtang666666-source.github.io/vue-xiuxiangame/`）。已移除旧的自定义域名 `xiuxian.wenzi.games` 与遗留的 `docs/`、`dev-dist/` 构建产物；如需自定义域名，请另行添加 CNAME 并把 Pages 源设为 Actions。
>
> 排查：若 push 后线上长时间不更新，先检查仓库 Settings → Pages → Source 是否为「GitHub Actions」；是则部署会自动生效，可能是浏览器 PWA 缓存，强制刷新/多刷一次即可。

## 7. 代码保护现状与可选升级
- 现状：`vite.config.js` 已启用 `vite-plugin-bundle-obfuscator`（变量重命名、字符串数组加密、控制流打平、防调试等），并做了 terser 压缩。能防普通查看，无法 100% 防前端破解（前端代码天然要下发给玩家）。
- 想更安全：
  1) 把源码仓库转 **私有**，部署改用 Cloudflare Pages / Netlify / Vercel（支持私有仓库自动部署），或另建“只放 dist”的公开仓库做 Pages。
  2) 把核心数值/结算逻辑逐步迁移到 **后端 API**，才是真正源码不可见。

## 8. 给接手的新 AI/开发者的话
- 项目是纯前端 Vue 游戏，先跑通 `npm install && npm run dev`，再动代码。
- 改数值先搜 `src/plugins/` 下对应文件；改界面先找 `src/views/` 对应页面；任何页面入口都在 `src/plugins/router.js`。
- 改完必须 `npm run build` + `git push`，线上才会更新。
- 不要动 `dist/`，不要动 `.github/workflows` 里的部署文件。
